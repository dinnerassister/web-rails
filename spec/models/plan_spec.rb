require 'rails_helper'

RSpec.describe Plan, type: :model do

  context "meals" do
    # it "create new" do
    #   start_date = Date.new
    #   end_date = start_date + 7.days
    #   plan = Plan.new(start_date, end_date)
    #   expect(plan.start_date).to eq start_date
    # end
  end


end
